=== FOLLOWING YOUR RECOMMENDATION - DEPLOY STEPS ===

You asked to follow this:
- Create repo work-checklist -> Private if Pro, Public if free
- Push, enable Settings > Pages > main branch / root
- Edit OPEN-FROM-GITHUB.bat with real URL
- Change Task Scheduler to run that bat

STATUS: I have DONE the local prep for you. Git repo is initialized and committed.

NOW YOU DO (5 minutes):

1. Go to github.com > New Repository
   - Name: work-checklist
   - Private: YES if you have GitHub Pro (recommended for office work)
           NO (Public) if free account (still safe, tasks not in repo)
   - DO NOT check "Add README"
   - Create

2. Push from your PC:
   OPTION A - Use my helper:
     Double-click PUSH_TO_GITHUB.bat in this folder, enter your username

   OPTION B - Manual:
     cd C:\path\to\work-checklist
     git remote add origin https://github.com/YOURUSERNAME/work-checklist.git
     git push -u origin main

   NOTE: Your workspace here is /home/user/work-checklist - download folder to your Windows PC first, then push. Or you can upload via GitHub web: drag all files (except .git folder) on GitHub repo page.

3. Enable Pages:
   - In your repo on GitHub, go to Settings > Pages (left sidebar)
   - Build and deployment: Source = Deploy from a branch
   - Branch = main, folder = / (root) -> Save
   - Wait 1-2 min, refresh, you'll see: https://YOURUSERNAME.github.io/work-checklist/
   - Open it - should show your checklist!

4. Update 9AM Auto-Open:
   - On your Windows PC, edit OPEN-FROM-GITHUB.bat with Notepad
   - Change line: set "URL=https://YOURUSERNAME.github.io/work-checklist/"
   - Save, double-click to test

5. Update Task Scheduler (for 9AM Mon-Fri):
   - Win+R -> taskschd.msc
   - Find your existing "Work Checklist 9AM" task (or create new)
   - Actions tab -> Edit -> Program = path to OPEN-FROM-GITHUB.bat
   - For direct URL method:
     Program: cmd
     Arguments: /c start https://YOURUSERNAME.github.io/work-checklist/
   - Triggers: Weekly, Mon Tue Wed Thu Fri, 9:00 AM
   - OK

6. Test:
   Right-click task > Run. Should open GitHub Pages URL in Chrome/Edge.

DONE! You now have:
- Local file version + GitHub cloud version identical
- 9AM auto-open from cloud (works even if you reinstall Windows)
- Access on iPhone: Open Pages URL in Safari > Share > Add to Home Screen

If you want, I can also add CNAME or make it a PWA installable app.

